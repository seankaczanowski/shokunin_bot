#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@lezer/generator/src/lezer-generator.cjs" "$@"
else
  exec node  "$basedir/../@lezer/generator/src/lezer-generator.cjs" "$@"
fi
